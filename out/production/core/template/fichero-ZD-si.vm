#define($CR)${writer.write(13)}#end
$sucursal.id|$fecha_cierre|$numero_detalles|${CR}
#foreach( $detalle in $detalles )
$foreach.count|$detalle.id_factura|$detalle.sku|$detalle.cantidad|$detalle.precio|$detalle.precio_factura|$parte|$detalle.surte|$detalle.codigo_color|$detalle.descuento|$detalle.factura|${CR}
#end